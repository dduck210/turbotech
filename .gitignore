/vendor/
/tailwindcss.exe
Thumbs.db
.DS_Store
*.log

# Local secrets (SMTP credentials, etc.) — never commit, see .env.example
/.env

# One-off DB backups taken before a migration (e.g. scripts/migrate-*.php) —
# these can contain plaintext data (e.g. pre-hash passwords), local-only.
/scripts/*_backup_*.sql

# Claude Code tooling
.claude/
